version: "3.8"
services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/app
    env_file:
      - .env

  asha_dashboard:
    build: ./asha_dashboard
    volumes:
      - ./asha_dashboard:/app
    ports:
      - "5000:5000"
    depends_on:
      - backend


